<metro:Window x:Class="CSMU_Knowledge_Check.MainWindow"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:metro="http://schemas.codeplex.com/elysium"
              xmlns:params="http://schemas.codeplex.com/elysium/params"
              params:Design.Theme="Dark"
              params:Design.AccentBrush="{x:Static metro:AccentBrushes.Blue}"
              params:Design.ContrastBrush="White"
              Title="CSMU Testing" Height="350" Width="525" Loaded="Window_Loaded_1">
    <Grid>
        <StackPanel>
            <Label HorizontalAlignment="Center" Content="{Binding QuestName}"/>
            <ListBox Name="selector" ItemsSource="{Binding}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Border BorderBrush="{x:Static metro:AccentBrushes.Blue}">
                                <Image Source="{Binding ImageSource}" Height="80" Width="80"/>
                            </Border>
                            <TextBlock Text="{Binding Answer}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</metro:Window>
